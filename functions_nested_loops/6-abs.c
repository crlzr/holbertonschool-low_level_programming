#include "main.h"

/**
 *_abs - computes the absolute value of an integer
 * @number: an int that takes a number from main
 * Return: number
 */

int _abs(int number)
{

if (number < 0)
{
	return (-number);
}
else
{
	return (number);
}
}
